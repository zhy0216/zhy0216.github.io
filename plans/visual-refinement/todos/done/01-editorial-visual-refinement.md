difficulty: medium
agent: inherit

# 首页与博客视觉精修

优先级：P1。执行选择：继承 `README.md`，当前解析为 Codex / `gpt-6-astra` / `xhigh`。

先完整阅读 [../../plan.md](../../plan.md)。任务条目共享 `src/index.css` 和首页组件文件，作为一个独立实现任务，在一个 worktree 内完成并形成一个最终 commit。

## T1 · 确定画面基线与视觉尺度

**要做什么**

- 在改动前启动本地预览，捕获首页首屏、作品区、首页文章区和博客首屏的 1440×1000 / 390×844 基线。使用减少动画固定对照，另外打开普通动画确认原始表现；不能将截图中的粘性导航或滚动状态误判为布局缺陷。
- 沿用黑白、钴蓝、Manrope / DM Mono 和几何图形，调整 `src/index.css` 的现有排版与间距规则。显示标题放松过紧字距，正文与交互信息提高字重、字号和对比，深浅背景各自保持清楚的蓝色状态。
- 在原有 CSS 区块内修改需要变化的样式，保持清楚的作用范围；不做无关架构重构，不在尾部积累一批互相覆盖的规则。

**预计修改的文件**

- `src/index.css`
- `plans/visual-refinement/evidence/`（精选原始截图，名称包含 before 与视口；只提交必要画面）

**验收条件**

- 有可供后续复核使用的修改前画面，截图文件可被任务 02 从本提交读取。
- 共享显示标题、正文、辅助标签、按钮和间距形成一致尺度；有信息意义的普通文字目标对比至少 4.5:1，大号文字至少 3:1。
- 字体继续使用已有本地资源，项目依赖与锁文件无需改动。

**前置依赖**：无。

## T2 · 精修首页首屏、作品与内容区

**要做什么**

- 在 `Nav`、`Hero` 和对应样式中调整首屏标题宽度、字号、换行、球体裁切和 CTA 位置；主标题、说明和主行动一眼可辨。去掉首屏说明 `.72em` 再缩小的效果，手机计算字号至少 15px。
- 优先调整 `.hero-orb-wrap`、遮罩和网格构图；必要时小幅改 `OrbitalScene` 的视角或材质，保持已有 WebGL 失败路径、减少动画模式、资源清理和像素比限制。
- 精修 `About`、`Work` / `ProjectArt`、`Blog`、`Lab`、`Contact` 的层级、对齐和留白；保留四种作品专属画面，提高作品说明和入口的可读性，利用表面明暗、细边框与小幅交互反馈丰富层次。
- 首页文章网格依据实际内容自适应，一篇、两篇、三篇都能合理填满；保留最多三篇的现有取数规则。如使用文章图像，只复用已有可选背景图元数据，无图的文章也要形成完整设计。
- 让卡片、按钮、导航的 hover 与键盘焦点反馈一致，手机入口可直接理解。移动菜单按钮约 44×44px，关闭菜单不留下不可见的可聚焦项；About 补充说明可选择常显，不能只靠鼠标悬停读取。
- 新的过渡尊重减少动画偏好；现有无限页脚和仓库数据行为保持原有作用，必要的标记调整不得改变筛选或链接语义。

**预计修改的文件**

- `src/App.jsx`
- `src/index.css`
- `src/RepositoryField.jsx`（仅当展示语义或焦点反馈需要少量标记调整；数据与状态逻辑不在范围内）

**验收条件**

- 1440×1000、768×1024、390×844 下，首屏和作品区都有清楚的视觉改善；在 320×740 补查长文本和 CTA，无相互遮挡或页面级横向溢出。
- 手机说明字号至少 15px；首屏高度不足时允许自然滚动，不用固定高度裁掉文字或操作。
- 四个作品仍指向原有 `/work/` 页面，项目图形仍各有辨识度；首页文章当前两篇不再留下空白第三列。
- 首页锚点、移动菜单、邮箱入口、仓库筛选可用，键盘焦点可见。仅检查邮箱入口，不发送邮件。
- 普通动画、减少动画和模拟 WebGL 创建失败都能看到完整重要内容、使用链接；无新增未捕获异常。

**前置依赖**：本文件 T1。

## T3 · 调整博客列表与文章阅读层级

**要做什么**

- 在 `BlogIndex` / `.blog-hero-copy` 中共同调整标题可用宽度、字号与行高，避免当前 1440px 宽度下四行、孤立单词和拥挤字距；让文章列表更早进入视野。
- 在 `BlogCard` 与 `.blog-page-grid` 中适配一、二、三、四篇内容，卡片标题按实际列宽调整；日期、标签、摘要、阅读入口形成明确顺序，处理换行后的边框。
- 用读者有用的真实信息替换 `MARKDOWN / BUILT AT DEPLOY TIME`；可使用已有日期、篇数和阅读时长字段，不编造数据。
- 微调 `BlogArticle` 的标题、背景遮罩、正文开始位置、正文行长和侧栏。保留已有平衡换行、衬线 h2/h3、代码复制、代码块内部滚动和文章交互示例。
- 同时检查 `css-z-index-is-a-tree` 与 `postgresql-speculative-insertion`，有图和无图文章均成立。

**预计修改的文件**

- `src/blog/BlogPage.jsx`
- `src/index.css`

**验收条件**

- 桌面博客首页标题形成有意图的两到三行排版，没有当前孤立的 “the” / “edge” 行；当前两篇文章完整利用内容宽度。
- 临时模拟一、三、四篇列表仍有合理布局，不提交假文章或为测试修改正式文章数据。
- 两篇实际文章在桌面和手机可读，背景不降低标题可辨认度；复制、返回与侧栏链接正常，代码块不撑宽整页。

**前置依赖**：本文件 T1、T2 的视觉尺度与首页实现。

## T4 · 构建与实施自检

**要做什么**

- 运行 `npm run build`、`git diff --check`，在生产预览中走查首页、博客列表、两篇文章和四个项目详情入口。
- 检查共享 CSS 是否改变项目页的标题、按钮和布局，只修复本轮造成的明确回归。
- 交付简短实施说明和基线截图列表，供任务 02 复核。不要把普通静态样式拆成镜像实现的单元测试，不新增浏览器依赖到项目。

**预计修改的文件**

- 本文件 T1–T3 已涉及的文件。
- 四个项目的 `src/{zebra,starwreck,better-trigger,sangota}/*.css`，仅限有证据的共享样式回归，默认无需修改。

**验收条件**

- 两个仓库校验命令通过，六个入口都能打开；真实浏览器检查与任何限制在最终汇报中清楚标出。
- 最终只有当前视觉范围相关文件、必要的精选基线截图；保留原有业务文案事实、资源、URL 和文章内容。
- 一个最终 commit，可以被协调器复核和集成；任务 02 应从此提交已集成的基线开始。

**前置依赖**：本文件 T2、T3。


## 实施与验收记录 · 2026-09-06

状态：T1–T4 全部完成，已归档；等待协调器集成。任务 02 从本任务已集成的提交继续，本任务没有修改 `plan.md` 或创建任务 02 的 `verification.md`。

### 实施范围

- `src/index.css`：在原有区块调整标题、正文、元信息、卡片、阅读容器和响应式规则；新增少量排版/颜色令牌。深底蓝色使用 `#8191ff`，浅底继续使用 `#0011e2`。首页和博客的字重局部提高，四个项目页保持原有字重。
- `src/App.jsx`：首屏说明取消二次缩小；文章按实际篇数分列并显示已有阅读时长；菜单使用 44×44px 按钮、`inert`、Escape 关闭和焦点返回；导航尊重减少动画及粘性导航偏移。
- `src/blog/BlogPage.jsx`：博客标题分成两个可换行的语义片段；文章按篇数组织列数（四篇为两列）；部署实现说明替换为真实篇数和最新日期，卡片显示真实阅读时长。
- `src/RepositoryField.jsx`：只为筛选按钮增加 `aria-pressed`。数据、筛选、状态、请求和 WebGL 逻辑均保持原样。
- 保留四种作品图形、项目 URL、本地 Manrope / DM Mono、全部正式文章和内容事实。未修改依赖、锁文件、项目详情 CSS、Three.js 场景逻辑或无限页脚逻辑。

### 逐项验收

| 条目 | 真实浏览器 / 命令证据 | 结果 |
| --- | --- | --- |
| T1 · 修改前基线 | 在基线 `60a3e30`、任何源码修改前，以 1440×1000 / 390×844、DPR 1、减少动画捕获下列八张 PNG；另外打开普通动画，球体 canvas 正常创建 | 通过 |
| T1 · 文字与颜色 | 计算样式确认首页说明桌面/平板 18px、手机 16px；正文通常 16–18px、标签及入口 10–11px，标题字距主要为 `-.04em` | 通过 |
| T1 · 对比 | 浅底正文/标签 `#60635d` 对 `#f4f4f0` 为 5.54:1；深底说明为 10.28:1；深底蓝色为 7.12:1；作品悬停表面上的蓝色为 6.02:1；蓝底白字 CTA 为 9.64:1。首页、两篇文章和悬停博客卡片的 axe 对比检查无失败项；渐变/伪元素的自动检查未决项另以画面和调色板复核 | 通过，自动检查有下述边界 |
| T1 · 字体 / 依赖 | 字体请求全部来自本地预览 origin；`package.json`、`package-lock.json` 和字体资源无差异 | 通过 |
| T2 · 响应式构图 | 1440×1000、768×1024、390×844、320×740 的首屏、作品、About、文章区、Lab、Contact 已走查；首页 `scrollWidth` 分别为 1440 / 768 / 390 / 320；说明与操作不相交，手机球体排在操作下方，矮屏自然增长 | 通过 |
| T2 · 项目 / 文章入口 | 四张专属作品画面和四个 `/work/` 链接保留；当前两篇文章各占内容区一半；首页最多三篇规则保持 | 通过 |
| T2 · 导航 / 键盘 / 邮箱 | 四个首页导航、首屏 CTA、发现按钮、返回顶部均定位到粘性导航下方；390 / 320 下菜单为 44×44px，Tab 可进入打开的菜单，Escape 返回按钮，关闭后不能聚焦隐藏项；按钮、文章、作品和邮箱入口有可见焦点。邮箱只检查入口和呈现，没有发送邮件 | 通过 |
| T2 · 仓库 | 阻断 GitHub API 后使用 2026.08.22 快照；ALL / ORIGINAL / FORKS / ARCHIVED 分别为 108 / 70 / 38 / 1，筛选数与显示数一致，键盘焦点更新读数，原链接保持 | 通过 |
| T2 · 动画 / 降级 | 1440 / 390 下，普通动画、减少动画、模拟 WebGL 创建失败均有完整重要内容和可用链接；普通球体连续截图发生变化，减少动画截图不变；普通无限页脚实际递增 LOOP，减少动画显示静态替代 | 通过 |
| T3 · 博客首屏 | 1440px 标题为两行，无孤立的 “the” / “edge”；列表区域起点约由 875px 提前至 587px；两篇卡片利用全部内容宽度 | 通过 |
| T3 · 内容数量 | 在生产预览中临时拦截文章数据模块响应，用已有文章副本模拟 1 / 2 / 3 / 4 篇，覆盖四种视口；桌面分别为单列 / 两列 / 三列 / 两行两列，手机纵排，换行边框连续；首页四篇输入只显示三篇 | 通过，无假文章或正式数据改动 |
| T3 · 两篇文章 | `css-z-index-is-a-tree` 与 `postgresql-speculative-insertion` 在四种视口均无页面横向溢出；有图/无图标题、正文、侧栏均已看图；衬线 h2/h3、平衡换行保留 | 通过 |
| T3 · 文章交互 | 两篇文章在四种视口分别验证剪贴板内容、COPIED 状态、Clipboard API 不可用时 SELECTED 回退、代码容器内部滚动、侧栏进入另一篇、ALL NOTES 返回列表 | 通过 |
| T4 · 六入口 / 回归 | 首页、博客和四个项目页在 1440 / 390 的三种模式组合共 36 次入口走查均返回 200；项目返回首页作品区正常；四个项目的 1440 / 390 前后首屏 PNG 逐字节相同 | 通过，无本轮项目页回归 |
| T4 · 异常 / 命令 | 所有完成的浏览器检查无 `pageerror`；按锁文件 `npm ci` 后 `npm run build` 通过（61 modules、六 HTML 入口），`git diff --check` 通过；仓库没有 test / lint / typecheck 脚本 | 通过 |

### 提交的基线截图

路径相对本归档文件；八张原始 PNG，共约 1.24 MiB。首屏滚动位置为 0；作品区和首页文章区将 section 顶部置于导航下方（桌面 76px、手机 65px），保留可见粘性导航。等待本地字体和页面资源完成后截图；GitHub API 被阻断以固定快照。

| 画面 | 1440×1000 | 390×844 |
| --- | --- | --- |
| 首页首屏 | [before-home-hero-1440x1000.png](../../evidence/before-home-hero-1440x1000.png) | [before-home-hero-390x844.png](../../evidence/before-home-hero-390x844.png) |
| 作品区 | [before-home-work-1440x1000.png](../../evidence/before-home-work-1440x1000.png) | [before-home-work-390x844.png](../../evidence/before-home-work-390x844.png) |
| 首页文章区 | [before-home-notes-1440x1000.png](../../evidence/before-home-notes-1440x1000.png) | [before-home-notes-390x844.png](../../evidence/before-home-notes-390x844.png) |
| 博客首屏 | [before-blog-index-1440x1000.png](../../evidence/before-blog-index-1440x1000.png) | [before-blog-index-390x844.png](../../evidence/before-blog-index-390x844.png) |

### 复核边界与交接

- 浏览器为 Playwright 1.63.0 / Chromium 153.0.8010.12，使用真实 headless 浏览器与桌面/手机视口；未覆盖 Safari、Firefox 或实体手机 GPU。浏览器依赖、脚本、改后截图及详细 JSON 留在项目外临时目录，位置随最终交接报告提供。
- GitHub API 人为阻断会产生预期资源失败日志；模拟无 WebGL 会产生 Three.js 的预期创建失败日志，均由既有降级路径处理，没有未捕获异常。未验证远端实时 API 可用性。
- axe 不能自动判定部分伪元素/渐变/图形叠层；上述比值为实际计算前景颜色与对应实色表面的对比，装饰图形另经截图检查，不把自动未决项写成自动通过。
- 修改前就存在：390px 的 Starwreck 标题右侧裁切；Sangota 页面宽度为 445px。前后项目截图相同，按本任务范围没有改项目专属 CSS。Sangota 768 / 320 的补查宽度为 793 / 380px，供 02 评估既有问题；这不影响首页和博客的无横向溢出结论。
- 本轮正式文章没有独立运行的嵌入式交互控件；保留文章中的代码示例，实际验证其复制和选中回退行为。无本任务 blocker。
