# Visual refinement · 独立浏览器复核

2026-09-06，任务 02。验证对象为任务 01 已集成提交 `8d71fbfa35eebf10683f92067100e93351bb8d4c`，对照基线为 `60a3e30`。依据 [方案](plan.md)、[任务 01 实施记录](todos/done/01-editorial-visual-refinement.md) 和 [任务 02](todos/done/02-responsive-visual-review.md) 执行。

**结论：本轮首页与博客的视觉改进成立，未发现需要修正的本轮回归；任务 02 只交付验证文档和截图。** Sangota 的窄屏整页溢出、Starwreck 的手机标题裁切仍存在，已独立复现为基线问题，不能据此写成“全站全部通过”。它们按约定留在本轮范围之外，无集成 blocker。

集成补记：任务提交 `e0335c4` 已经原任务 agent rebase、协调器独立构建与 diff 校验后快进合入 `master`，任务资源已清理。以下保留实施交付时的验证记录；完整流程见 [执行结果](plan.md#执行结果)。

## 验证来源与环境

- 在当前任务 worktree 按锁文件执行 `npm ci`、构建，再启动独立生产预览 `http://127.0.0.1:4327`。没有使用协调器的测量 JSON 作为本报告的实测结果。
- 复用了任务 01 的临时浏览器脚本，但复制到任务 02 的独立目录、更换预览地址后重新执行；另写了链接、触控、动画、降级组件与实际背景取样脚本。所有浏览器、截图、测量均为本任务新运行所得。
- 使用 Playwright **1.63.0 / Chromium 153.0.8010.12**，macOS 上的真实 headless 浏览器，DPR 1。四个视口为 **1440×1000、768×1024、390×844、320×740**；另在两个手机尺寸启用 `isMobile` / `hasTouch`，确认 `pointer: coarse` 后实际执行 tap。
- 静态对照采用 `prefers-reduced-motion: reduce`，等待本地字体、资源加载及页面稳定；新浏览器上下文阻断 GitHub API，固定使用已有快照。首页首屏滚动位置为 0；作品区、首页文章区将 section 顶部放到粘性导航下方，桌面 76px、手机 65px，与已有 before 捕获口径一致。
- 另以 `git archive 60a3e30` 导出到项目外临时目录，使用同一套已安装依赖构建，在独立端口 4328 预览。没有切换分支或修改 master。基线与当前版本的四个项目页在四视口下共 **16 对首屏 PNG 逐字节相同**。
- 已读相关 JSX、CSS、文章数据/渲染、邮箱保护和仓库快照路径。worktree 的语义检索返回 `INDEX_MISSING`，按约定使用精确检索，没有创建、重建或删除索引。

## T1 · 页面与视觉

### 页面和横向尺寸

表中数字为 `document.documentElement.scrollWidth`；各列的 `clientWidth` 分别为 1440 / 768 / 390 / 320。八个页面在四视口共 **32 个页面/视口组合**均打开并捕获画面，两篇文章另外检查正文与代码容器。所有页面返回 200。

| 页面 | 1440×1000 | 768×1024 | 390×844 | 320×740 | 结果与边界 |
| --- | ---: | ---: | ---: | ---: | --- |
| `/` | 1440 | 768 | 390 | 320 | 首屏、About、四张作品卡、文章区、仓库区、联系区已检查；无整页溢出 |
| `/blog/` | 1440 | 768 | 390 | 320 | 标题、卡片、菜单/返回入口无遮挡；无整页溢出 |
| `/blog/?post=css-z-index-is-a-tree` | 1440 | 768 | 390 | 320 | 无背景图；标题、正文、代码、其他文章入口可读 |
| `/blog/?post=postgresql-speculative-insertion` | 1440 | 768 | 390 | 320 | 背景图成功加载，标题/摘要可辨认；无整页溢出 |
| `/work/zebra/` | 1440 | 768 | 390 | 320 | 标题、黑白条纹身份、首屏与返回入口保持 |
| `/work/starwreck/` | 1440 | 768 | 390 | 320 | 无整页溢出；**390 / 320 标题右侧裁切，基线问题** |
| `/work/better-trigger/` | 1440 | 768 | 390 | 320 | 标题、蓝色执行图形、首屏与返回入口保持 |
| `/work/sangota/` | 1440 | **793** | **445** | **380** | **三个窄视口不满足整页宽度目标，基线问题**；标题、图像与返回入口可用 |

Starwreck 的 h1 右边界实测为 431.41px（390 视口）和 366.92px（320 视口），虽被 hero 裁切而未撑宽整页，仍按标题裁切记录失败。Sangota 的对应原始基线宽度也是 793 / 445 / 380px，装饰 `.sg-hero-art` 的已知溢出保持原样。项目专属源码、CSS 和素材均未修改；16 对相同 PNG 支持“无本轮项目首屏视觉回归”，不代表项目页其他功能或原有排版都合格。

### 首页与博客观察

| 检查项 | 独立观察 / 测量 | 结论 |
| --- | --- | --- |
| 首页说明 | 1440 / 768 为 **18px**，390 / 320 为 **16px**，计算字重 400；原始手机 before 为 11.52px | 达到手机至少 15px 的目标 |
| 首屏构图 | 桌面标题收成两行，字距放松、浅蓝强调更可辨认，球体缩小并右移；手机说明换行可读，CTA 在球体上方 | 与 before 有明确可见改善 |
| 首屏边界 | 四视口测量说明、行动组与 hero 矩形；行动组在说明之后并包含于 hero 内，按钮不出右边界；手机行动组底部不超过球体顶部 | 无主要文字/行动相互遮挡；320 矮屏自然滚动 |
| About | 补充介绍无需 hover 就可见；桌面侧栏、平板/手机纵排，标题与正文层级清楚；小屏统计区继续换行排布 | 通过 |
| 四张作品卡 | 逐张检查标题、说明、标签和入口；Zebra 条纹、Starwreck 舰船、Better Trigger 执行图、Sangota 人物保持辨识度。桌面图文比例更均衡，说明与入口更清楚；手机标签自然换行、入口单列 | 通过；图像本身的艺术裁切与正常视口截断不算文字丢失 |
| 首页文章区 | 1440px 两张卡片宽度各 **658.5px**，中间 1px 分隔，占满 1318px 网格；阅读时长、摘要、标签与 READ 层级完整 | 原空白第三列消失 |
| 博客 hero | 1440px 实测标题高度约 209.55px / 行高 104.78px，即 **两行**；列表区域起点约 **587px**，before 为 875px | 原四行拥挤排版消失，首屏露出两篇文章 |
| 平板/窄手机博客 | 768 / 390 的 hero 为两行；320 为三行，长标题和顶部 HOME / BLOG 链接可用；卡片按单列排布 | 通过；两到三行目标限定桌面，不强迫所有尺寸同样换行 |
| 仓库与联系区 | 标题、说明、筛选按钮、仓库名、选中读数与邮件入口逐区检查；手机筛选为 2×2、联系 CTA 随标题向下排列 | 无本轮内容或行动遮挡 |
| 两篇文章 | 四视口检查首屏和正文；衬线 h2/h3 保留，平板/手机其他文章入口移到正文之后，有图/无图页面均可读 | 通过；实际文章内没有独立运行的嵌入式交互控件 |

### 同视口前后画面

下列八张 after 均为本任务新截图，保留原尺寸 PNG。没有提交整页长图或拼图。

| 画面 | 1440×1000 before | 1440×1000 after | 390×844 before | 390×844 after |
| --- | --- | --- | --- | --- |
| 首页首屏 | [before](evidence/before-home-hero-1440x1000.png) | [after](evidence/after-home-hero-1440x1000.png) | [before](evidence/before-home-hero-390x844.png) | [after](evidence/after-home-hero-390x844.png) |
| 作品区 | [before](evidence/before-home-work-1440x1000.png) | [after](evidence/after-home-work-1440x1000.png) | [before](evidence/before-home-work-390x844.png) | [after](evidence/after-home-work-390x844.png) |
| 首页文章区 | [before](evidence/before-home-notes-1440x1000.png) | [after](evidence/after-home-notes-1440x1000.png) | [before](evidence/before-home-notes-390x844.png) | [after](evidence/after-home-notes-390x844.png) |
| 博客首屏 | [before](evidence/before-blog-index-1440x1000.png) | [after](evidence/after-blog-index-1440x1000.png) | [before](evidence/before-blog-index-390x844.png) | [after](evidence/after-blog-index-390x844.png) |

补充保留两篇实际文章的代表画面：[CSS 文章 / 1440×1000](evidence/after-article-css-1440x1000.png)、[PostgreSQL 文章 / 390×844](evidence/after-article-postgresql-390x844.png)。文章画面只有本轮 after，没有声称存在未捕获的 before。

### 文字对比度

读取实际元素计算颜色，以 sRGB 相对亮度计算 `(L亮 + 0.05) / (L暗 + 0.05)`；普通文字按 4.5:1，大号文字按 3:1。以下为主要正文与蓝色交互，未用装饰线替代检查。hover 等待过渡完成后取样。

| 元素 / 状态 | 前景 | 背景 | 比值 |
| --- | --- | --- | ---: |
| 首页说明 | `#b8bab4` | `#070707` 实色底 | 10.28:1 |
| 深底蓝色主标题 / 博客当前导航 | `#8191ff` | `#070707` | 7.12:1 |
| About 正文、首页/博客卡片摘要与标签、Lab 说明 | `#60635d` | `#f4f4f0` | 5.54:1 |
| 作品说明 | `#b8bab4` | `#111211` | 9.59:1 |
| 作品入口 hover | `#8191ff` | `#1a1c22` | 6.02:1 |
| 首屏蓝底 CTA / hover | `#ffffff` | `#0011e2` / `#2638ff` | 9.64 / 6.79:1 |
| 首页/博客 READ、文章正文链接 | `#0011e2` | `#f4f4f0` | 8.74:1 |
| 文章卡片 READ hover | `#8191ff` | `#070707` | 7.12:1 |
| 文章卡片摘要 hover | `#a3a49f` | `#070707` | 8.03:1 |
| 仓库选中名称 | `#ffffff` | `#0011e2` | 9.64:1 |
| 仓库读数说明、邮箱保护入口 | `#b8bab4` | `#070707` | 10.28:1 |
| 文章正文 | `#3f403c` | `#f4f4f0` | 9.48:1 |
| 代码工具栏语言 / COPY | `#aeb6d2` / `#b9c3e5` | `#171b28` | 8.52 / 9.81:1 |

对首页图形/渐变与 PostgreSQL 背景图，另在临时 DOM 隐去文字、保留构图与背景，等待重绘后取原文字矩形内的实际合成背景像素，使用该区域的最低比值。这些无字取样图没有作为成品截图提交。

| 实际背景取样 | 1440 | 768 | 390 | 320 | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| 首页蓝色大标题 | 7.08 | 6.96 | 7.00 | 6.87 | ≥ 3 |
| 首页说明 | 10.22 | 9.42 | 10.11 | 9.92 | ≥ 4.5 |
| PostgreSQL 标题 | 10.59 | 11.07 | 12.50 | 13.22 | ≥ 3 |
| PostgreSQL 摘要 | 11.34 | 9.97 | 9.99 | 9.47 | ≥ 4.5 |
| PostgreSQL 返回入口 | 10.22 | 10.22 | 10.17 | 10.16 | ≥ 4.5 |
| PostgreSQL 日期 | 7.90 | 7.86 | 7.85 | 7.81 | ≥ 4.5 |

另执行 axe 的 `color-contrast` 规则：首页、两篇文章和博客卡片 hover 均为 0 个自动失败项；仍有 257 / 48 / 65 / 6 个自动未决节点，涉及伪元素、图形或其他自动判定边界。以上表格是实际取样结果，不把这些未决节点改写成自动通过，也不宣称完整 WCAG 审计。静态背景像素取样覆盖上述四视口的固定帧，不代表每一种 GPU 或动画帧的穷尽对比验证。

## T2 · 交互、数据数量与降级

### 实际操作

| 检查 | 执行范围与结果 |
| --- | --- |
| 首页锚点 | 四视口依次点击 About / Work / Blog / Lab；目标 section 顶部落在导航下方，偏差 ≤ 2px。首屏 EXPLORE、SCROLL TO DISCOVER、字标返回顶部、联系区 BACK TO TOP 均实际点击通过 |
| 移动菜单 | 390 / 320 测量按钮为 **44×44px**；tap 开启并选择 Work，键盘 Enter 开启、Tab 进入 About、Enter 完成导航，Escape 关闭并把焦点还给按钮。关闭后逐次 Tab 检查，隐藏菜单不接收焦点；强制调用隐藏项 focus 也不能进入 |
| 键盘焦点 | 菜单、CTA、作品文字入口、文章卡片、仓库及邮箱入口检查实际焦点反馈；文章卡片通过键盘 Enter 进入。仓库以蓝底/光环显示选择；复制按钮的点击与状态结果见下一项 |
| 作品与文章链接 | 1440 / 390 实际点击四张作品的图像和 VIEW PROJECT，共 16 次进入并点击返回 `/#work`；首页与博客的两篇文章入口共 8 次激活，ALL NOTES 返回成功。两种首页“全部文章”入口、博客字标/HOME/页脚返回首页均通过 |
| 两篇文章复制 | 四视口逐篇点击首个代码块 COPY，检查 `COPIED` 和实际剪贴板内容；临时关闭 Clipboard API 后再点击，检查 `SELECTED` 及选中文字。其他文章侧栏/末尾链接进入另一篇，随后返回列表；均通过 |
| 长代码块 | 浏览器内对代码 `pre` 执行横向滚动并检查 `scrollLeft`。390px 下容器宽 315px、最长内容 CSS 565px / SQL 500px；320px 下容器宽 254px，最长内容仍为 565 / 500px，均可滚到 `scrollLeft=100`。1440 / 768 下现有代码无需横向滚动；所有文章整页宽度保持视口宽 |
| 文章背景与示例 | PostgreSQL 背景请求成功，CSS 文章无背景。检查渲染 DOM 和 Markdown：两篇文章的交互控件只有代码复制按钮，没有可执行的独立 demo；没有把代码示例当成运行中的交互测试 |
| 仓库 API 不可用 | 新上下文阻断 API 后使用 **SNAPSHOT / 2026.08.22**；四视口 ALL / ORIGINAL / FORKS / ARCHIVED 分别显示 **108 / 70 / 38 / 1**，数量与按钮一致，`aria-pressed` 正确。390px 另模拟 HTTP 503，得到相同快照/筛选结果 |
| 仓库选择与链接 | 鼠标 hover、键盘 focus 更新选中名称和读数；切换筛选清除旧选择。Enter 实际打开仓库目标页，弹窗 URL 与节点及 OPEN REPOSITORY 的 href 一致；远端 GitHub 页面由临时路由拦截，没有据此宣称远端服务可用 |
| 邮件入口 | 四视口确认受保护地址正确显示、入口可聚焦；1440 / 390 复验键盘焦点。遵照任务只验证保护入口，未激活邮件客户端、未发送消息 |

精选状态：[320×740 的键盘菜单焦点](evidence/after-menu-keyboard-320x740.png)。菜单作为开启的覆盖层遮住后方页面是预期行为；关闭后内容和焦点顺序恢复。

### 1–4 篇文章

在浏览器中临时拦截生产 `blogs-*.js` 数据模块响应，使用已有两篇文章的副本和临时唯一 slug 输入 1 / 2 / 3 / 4 篇。分别检查首页与博客、全部四视口，共 **32 个列表/数量/视口组合**；没有修改 `blogs/`、`src/blogs.js` 或构建产物里的正式数据。

| 输入篇数 | 首页 / 1440 | 博客 / 1440 | 768 / 390 / 320 |
| ---: | --- | --- | --- |
| 1 | 单列，1318px | 单列，1318px | 各自单列填满可用宽度 |
| 2 | 两列，各 658.5px | 两列，各 658.5px | 两张卡片纵排 |
| 3 | 三列，各约 438.66px | 三列，各约 438.66px | 三张卡片纵排 |
| 4 | **只显示三篇**，三列 | **两行两列**，每张 658.5×390px | 首页三张 / 博客四张纵排 |

桌面卡片同排高度一致；手机按内容自然增高，标题、标签和 READ 不互相挤占。末列右边缘与网格右边缘相差 < 1px，跨行分隔连续，无新增整页溢出。精选 [1440×1000 的四篇临时列表](evidence/after-blog-four-notes-1440x1000.png) 仅证明布局，重复标题明确来自本次浏览器模拟，最终正式页面仍为两篇文章。

### 普通动画、减少动画、WebGL 初始化失败

首页、博客列表、两篇文章和四个项目页，在 **1440×1000 / 390×844 × 三种模式**共 48 个入口/状态组合中运行并检查导航；所有组合无 `pageerror`。390 首页另外覆盖“reduce + WebGL 失败”的组合。

| 状态 | 实际结果 |
| --- | --- |
| 普通动画 + WebGL | 首页球体连续截图字节发生变化；额外间隔 500ms 捕获四帧，标题和 CTA 矩形稳定，所查看的帧没有明显闪动或遮挡；逐张 hover 四个作品并等待效果。无限页脚实际从 LOOP 01 进入 LOOP 02，返回顶部可完成 |
| 减少动画 + WebGL | 首页球体连续截图一致；重要文字和入口保持可见，滚动/菜单可用；无限页脚 canvas 隐藏，静态替代可见 |
| 普通动画 + WebGL 初始化失败 | 临时将 canvas 的 WebGL context 创建返回 null。首页球体和仓库使用 CSS 降级，Zebra 使用条纹、Starwreck 使用暗星云背景、Better Trigger 使用蓝线；Sangota 首屏主要为图片/CSS，无新增 WebGL 依赖。对应 fallback class 存在，相关 WebGL canvas 数为 0；仓库筛选/选中、作品返回和文章复制/返回仍可操作 |

精选 [390×844 的 WebGL 失败首页](evidence/after-home-webgl-fallback-390x844.png)。这项检查模拟**初始化失败**，未模拟运行中的 context lost 或真实 GPU 驱动故障。

控制台记录中的 `Failed to load resource: net::ERR_FAILED` 来自人为阻断 GitHub API；HTTP 503 检查也有预期资源错误。无 WebGL 时记录 `THREE.WebGLRenderer: Error creating WebGL context.`，由既有 catch/fallback 处理。普通动画/减少动画没有其他新增控制台 error 或未捕获页面异常。未据此推断远端实时 API、外部项目或长时间运行全部正常。

## T3 · 交付与命令

本轮没有发现有证据的新视觉回归，因此没有源码修复；前述两个项目页基线问题仅记录。交付为本报告、**13 张精选 after PNG（1,862,115 bytes，约 1.78 MiB）**、任务 02 归档及队列状态更新。八张原有 before 保持原样；`plan.md` 留给协调器追加结果。

| 命令 / 检查 | 结果 |
| --- | --- |
| `npm ci` | 通过，按锁文件安装 69 个包；依赖与锁文件无改动 |
| `npm run build`（开始与交付前） | 均通过；Vite 6.4.3，61 modules，六个 HTML 入口，无构建警告 |
| `git diff --check`（交付前） | 通过 |
| `capture.mjs after` / `capture-baseline.mjs after` | 当前/原始基线独立捕获完成；16 对项目首屏 PNG 相同 |
| `verify.mjs` | 32 组通过、0 失败：四视口首页/文章、数量变化与首页模式检查 |
| `final-browser.mjs` | 六入口 × 两视口 × 三模式共 36 次入口检查，以及动画、焦点和对比检查通过 |
| `supplement.mjs` | 链接激活、触控/键盘菜单、HTTP 503、普通动画观察、背景/控件检查通过；对比复跑保留前景 alpha 合成，35 个样本达到对应阈值 |
| `evidence-checks.mjs` | 两篇文章 × 两视口 × 三模式另 12 次检查、降级组件与四篇状态截图完成 |
| `pixel-check.mjs` + `pixel-review.py` | 四视口真实合成背景共 24 个文字样本达到对应阈值 |
| `menu-evidence.mjs` | 等待菜单实际可见后执行键盘 Tab，焦点截图与 Escape 关闭通过 |
| 正式数据与文件范围 | 无占位文章、源码、依赖、字体、素材或无关文件改动；仓库没有 test / lint / typecheck 脚本，未声称执行 |
| 清理与 Git（实施交付时） | 本任务启动的浏览器、4327 当前预览和 4328 临时基线预览均关闭；只创建一个本地任务提交，等待协调器集成，未 push / 发布 / rebase / merge |

临时工具曾做两项取样校正：菜单截屏最初在 React 显示菜单前就发送 Tab，改为等待可见状态；初次背景取样尚有文字残留，改为显式隐藏文字并等待重绘后重测。两者是浏览器脚本时序/取样问题，未作为产品回归修源码。报告中使用最终有效结果。

复验工具留在本机项目外目录 `/tmp/visual-refinement-02.XFXS8o/`，包含上述脚本、日志和 JSON；Playwright 使用 `/private/tmp/better-trigger-browser.P2DEpN/node_modules/playwright/`，axe 复用任务 01 的临时安装。临时文件不作为交付链接或长期审阅前提，本报告保留必要测量，所有交付截图均使用仓库内相对链接。

### 真实限制

- 未测 Safari、Firefox、实体手机、不同 DPR、实际触屏/GPU 驱动和浏览器缩放；移动检查是 Chromium 视口及触控模拟。
- 未做性能基准、长时间稳定性、完整辅助技术/WCAG 审计或项目页内部功能穷尽测试；普通动画结论限于本轮操作和采样观察。
- 未验证远端 GitHub 实时 API、外部项目文档/游戏的服务状态、邮件客户端唤起或实际发送；没有运行并不存在的文章内独立 demo。
- Sangota 窄屏整页溢出与 Starwreck 手机标题裁切为**已复现的原有失败项**，不是未测项或本轮已修复项。其余“通过”均限定在本报告明确列出的页面、尺寸、入口与操作中。
